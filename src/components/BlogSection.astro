---
import BlogCard, { type BlogPost } from "@/components/BlogCard.astro";
import { blogPosts } from "@/data/blogPosts";
---

<section class="py-20 bg-[#fefefe]" id="blog">
  <div class="max-w-6xl mx-auto px-6">
    <div class="text-center mb-16 mt-10">
      <span class="inline-block text-primary font-medium mb-2">
        BLOG DE INVERSIÓN INMOBILIARIA
      </span>
      <h2
        class="font-serif text-3xl font-bold tracking-tight sm:text-4xl text-foreground"
      >
        Guía para <span class="text-primary"
          >comprar terrenos en Costa Rica</span
        >
      </h2>
      <p class="mt-4 text-lg text-muted-foreground max-w-2xl mx-auto">
        Leé nuestros artículos sobre <strong
          >cómo invertir en terrenos en La Fortuna de San Carlos</strong
        >, trámites legales, <strong>financiamiento 100% aprobado</strong> y estrategias
        para comprar propiedad en zonas con alta plusvalía.
      </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      {
        blogPosts
          .sort(
            (a, b) => new Date(b.date).getTime() - new Date(a.date).getTime()
          )
          .map((post) => <BlogCard post={post} />)
      }
    </div>

    <div class="flex justify-center mt-10">
      <a
        href="/blog"
        class="text-sm font-medium border border-[#ffcc66] text-[#1C1C1C] hover:text-black px-5 py-2 rounded-md hover:bg-[#ffcc66] transition-colors duration-300"
      >
        Leer más artículos del blog
      </a>
    </div>
  </div>
</section>
