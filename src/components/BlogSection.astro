---
import BlogCard, { type BlogPost } from "@/components/BlogCard.astro";
import { blogPosts } from "@/data/blogPosts";
---

<section class="py-20 bg-[#fefefe]" id="blog">
  <div class="max-w-6xl mx-auto px-6">
    <div class="text-center mb-12">
      <h2
        class="text-3xl sm:text-4xl font-bold text-[#1C1C1C] font-[Montserrat]"
      >
        Invertí mejor: consejos para comprar terrenos en Costa Rica
      </h2>
      <p class="text-gray-600 mt-4 max-w-2xl mx-auto">
        Explorá artículos sobre inversión inmobiliaria, trámites legales,
        financiamiento y consejos prácticos para comprar terrenos en La Fortuna
        y otras zonas de alto crecimiento en Costa Rica.
      </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      {blogPosts.map((post) => <BlogCard post={post} />)}
    </div>

    <div class="flex justify-center mt-10">
      <a
        href="/blog"
        class="text-sm font-medium border border-[#ffcc66] text-[#1C1C1C] hover:text-black px-5 py-2 rounded-md hover:bg-[#ffcc66] transition-colors duration-300"
      >
        Leer más artículos del blog
      </a>
    </div>
  </div>
</section>
