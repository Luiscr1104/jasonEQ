---
import { Star } from '@/components/icons';

const testimonials = [
  {
    id: 1,
    content:
      "Jason me ayudó a encontrar el terreno perfecto en Altos del Guayacán. Me explicó todo con paciencia, incluso el tema del financiamiento sin banco. ¡Una experiencia excelente!",
    author: "Argiere Chinchicha",
    role: "Inversionista en Altos del Guayacán",
    avatar: "AC",
  },
  {
    id: 2,
    content:
      "No sabía por dónde empezar hasta que hablé con Jason. Me acompañó en todo: visitas, trámites, planos. Hoy soy dueña de un lote en La Fortuna, gracias a su guía.",
    author: "Pamela Rodríguez",
    role: "Propietaria en La Fortuna",
    avatar: "P",
  },
  {
    id: 3,
    content:
      "Gracias a Jason adquirí un terreno en Condominio El Establo. El proceso fue rápido, seguro y sin bancos. Su conocimiento sobre Ecoquintas marcó la diferencia.",
    author: "Marcos Solís",
    role: "Inversionista en El Establo",
    avatar: "MS",
  },
];

---

<section id="testimonios" class="py-20 bg-[#FAFAFA]">
  <div class="container px-6 sm:px-8 md:px-12 max-w-7xl mx-auto">
    <!-- Encabezado -->
    <div class="text-center mb-16">
      <span class="inline-block text-primary font-medium mb-2">EXPERIENCIAS REALES</span>
        <h2 class="font-serif text-3xl font-bold tracking-tight sm:text-4xl text-foreground">
          Lo que opinan <span class="text-primary">quienes ya compraron</span>
        </h2>
        <p class="mt-4 text-lg text-muted-foreground max-w-2xl mx-auto">
          Opiniones reales de personas que encontraron su terreno ideal gracias a mi asesoría personalizada como parte de Grupo Ecoquintas.
        </p>
    </div>

    <!-- Tarjetas -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {testimonials.map((testimonial, index) => (
        <div
          class={`bg-white rounded-2xl border border-border overflow-hidden shadow-sm ${
            index === 1 ? 'md:-mt-8' : ''
          }`}
        >
          <div class="pt-8 px-6 relative">
            <div class="flex mb-4">
              {[...Array(5)].map((_, i) => (
                <Star key={i} class="h-5 w-5 fill-primary text-primary" />
              ))}
            </div>
            <p class="text-foreground relative z-10">"{testimonial.content}"</p>
          </div>

          <div class="border-t border-border px-6 pt-6 pb-8 mt-6">
            <div class="flex items-center gap-4">
              <!-- Avatar con iniciales -->
              <div class="w-10 h-10 flex items-center justify-center rounded-full bg-primary text-white text-sm font-semibold">
                {testimonial.avatar}
              </div>
              <div>
                <p class="font-medium text-foreground">{testimonial.author}</p>
                <p class="text-sm text-muted-foreground">{testimonial.role}</p>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
