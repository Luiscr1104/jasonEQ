---
const images = [
  {
    url: "/images/galeria/ecoquintas-1.webp",
    caption: "Condominio El Establo",
    alt: "Vista aérea del Condominio El Establo rodeado de zonas verdes en La Fortuna de San Carlos",
  },
  {
    url: "/images/galeria/ecoquintas-2.webp",
    caption: "Condominio Perla 1",
    alt: "Terreno en Condominio Perla 1 con paisaje montañoso en el fondo",
  },
  {
    url: "/images/galeria/ecoquintas-3.webp",
    caption: "Condominio Gaia",
    alt: "Vista panorámica del Condominio Gaia con lotes delimitados",
  },
  {
    url: "/images/galeria/ecoquintas-4.webp",
    caption: "Condominio El Trapiche",
    alt: "Entrada del Condominio El Trapiche con camino asfaltado",
  },
  {
    url: "/images/galeria/ecoquintas-5.webp",
    caption: "Condominio Nakury 1",
    alt: "Terreno amplio del Condominio Nakury 1 con cielo despejado",
  },
  {
    url: "/images/galeria/ecoquintas-2.webp",
    caption: "Condominio Gaia",
    alt: "Otra vista del Condominio Gaia con vegetación abundante",
  },
];
---

<!-- Encabezado -->
<div class="text-center mb-16 mt-20">
  <span class="inline-block text-primary font-medium mb-2">
    NUESTROS DESARROLLOS
  </span>
  <h2
    class="font-serif text-3xl font-bold tracking-tight sm:text-4xl text-foreground"
  >
    Galería de <span class="text-primary">Terrenos</span>
  </h2>
  <p class="mt-4 text-lg text-muted-foreground max-w-2xl mx-auto">
    Explora nuestros desarrollos y descubre el terreno ideal para tu próxima
    inversión o proyecto.
  </p>
</div>

<ul class="gallery">
  {
    images.map(({ url, caption, alt }) => (
      <li>
        <a href="#">
          <figure>
            <img src={url} alt={alt} />
            <figcaption>{caption}</figcaption>
          </figure>
        </a>
      </li>
    ))
  }
</ul>

<style>
  * {
    box-sizing: border-box;
  }

  .gallery {
    list-style: none;
    margin: 0 auto;
    padding: 0;
    display: grid;
    gap: 0.5rem;
    grid-template-columns: repeat(auto-fit, minmax(20rem, 1fr));
    max-width: 100%;
    width: 70rem;
  }

  figure {
    margin: 0;
    position: relative;
    overflow: hidden;
    border-radius: 0.75rem;
    border: 1px solid hsl(var(--primary));
  }

  img {
    width: 100%;
    display: block;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    transition: transform 1000ms;
  }

  figure::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    width: 200%;
    height: 200%;
    background: hsla(var(--primary), 0.4);
    transform-origin: center;
    opacity: 0;
    transform: scale(2);
    transition: opacity 300ms;
  }

  figcaption {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 1rem;
    color: white;
    font-size: 1.2rem;
    z-index: 1;
    opacity: 0;
    transition:
      opacity 600ms,
      transform 600ms;
    font-weight: 600;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.5);
  }

  a:is(:hover, :focus) figure::after {
    opacity: 1;
  }

  a:is(:hover, :focus) figcaption {
    opacity: 1;
    transition: opacity 600ms;
  }

  @media (prefers-reduced-motion: no-preference) {
    figcaption {
      transform: translate3d(0, 2rem, 0);
    }

    figure::after {
      border-radius: 50%;
      opacity: 1;
      transform: scale(0);
      transition: transform 900ms;
    }

    a:is(:hover, :focus) figure::after {
      transform: scale(2.5);
    }

    a:is(:hover, :focus) figcaption {
      opacity: 1;
      transform: translate3d(0, 0, 0);
      transition:
        opacity 600ms 400ms,
        transform 600ms 400ms;
    }

    a:is(:hover, :focus) img {
      transform: scale(1.2);
    }
  }
</style>
