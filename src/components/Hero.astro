<section
  class="relative w-full overflow-hidden bg-background min-h-[calc(100vh-4rem)] sm:aspect-[16/9]"
  aria-labelledby="hero-title"
>
  <!-- Imagen de fondo + gradiente -->
  <img
    src="https://imagedelivery.net/Lh5ivcu1Gl9SRBAdYRSP2g/114bc3e7-693a-463d-e011-6c7bc4889700/public"
    alt="Compra terrenos en La Fortuna de San Carlos"
    width="1500"
    height="1000"
    loading="eager"
    fetchpriority="high"
    class="absolute inset-0 w-full h-full object-cover brightness-[0.9] z-0"
  />
  <div class="absolute inset-0 bg-gradient-to-r from-black/80 to-black/40 z-0">
  </div>

  <!-- Contenido sobre la imagen -->
  <div class="absolute inset-0 z-10 flex items-center pt-[4rem] px-4 sm:px-8">
    <header
      class="text-white space-y-5 max-w-3xl mx-auto text-center sm:text-left"
    >
      <!-- Badge -->
      <div
        class="inline-flex items-center rounded-full border border-white/20 bg-white/10 px-3 py-1 text-xs sm:text-sm backdrop-blur-sm"
      >
        <span
          class="mr-2 rounded-full bg-primary px-2 py-0.5 text-[10px] sm:text-xs font-medium"
        >
          Oficial
        </span>
        Asesor Autorizado de Grupo Ecoquintas
      </div>

      <!-- Título -->
      <h1
        id="hero-title"
        class="font-serif text-xl sm:text-4xl md:text-5xl font-bold tracking-tight drop-shadow-lg leading-tight sm:leading-snug"
      >
        <strong>Terrenos en La Fortuna de San Carlos</strong> con
        <span class="text-primary">financiamiento 100%</span> y
        <span class="italic">asesoría gratuita</span>
      </h1>

      <!-- Beneficios -->
      <ul
        class="flex flex-wrap justify-center sm:justify-start gap-2 sm:gap-3 text-sm sm:text-base"
      >
        {
          ["100% financiadas", "Sin prima", "Sin fiador"].map((b) => (
            <li class="bg-white/10 border border-white/20 text-white px-4 py-1.5 rounded-full backdrop-blur-sm shadow-sm hover:bg-white/20 transition">
              {b}
            </li>
          ))
        }
      </ul>

      <!-- Descripción -->
      <p class="text-white/90 text-sm sm:text-base md:text-lg leading-relaxed">
        <strong>Invertí con acompañamiento profesional</strong> y
        <strong>sin bancos ni fiador</strong>.
        <span class="hidden sm:inline">
          Aprovechá precios exclusivos, ubicación estratégica y planos listos.
        </span>
      </p>

      <!-- Botones -->
      <nav class="flex flex-col sm:flex-row gap-3 pt-4 sm:pt-6">
        <a
          href="/propiedades"
          class="text-white bg-primary hover:bg-primary/80 px-5 py-2.5 rounded-md text-sm sm:text-base font-medium text-center transition"
        >
          Ver terrenos
        </a>
        <a
          href="#contacto"
          class="bg-white/10 text-white border border-white/20 hover:bg-white/20 px-5 py-2.5 rounded-md text-sm sm:text-base font-medium text-center transition"
        >
          Contactarme
        </a>
      </nav>
    </header>
  </div>
</section>

<!-- Animaciones -->
<style>
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  .animate-fade-in {
    animation: fade-in 1.2s ease-in-out both;
  }

  @keyframes fade-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fade-up {
    animation: fade-up 1.2s ease-out both;
  }
</style>
